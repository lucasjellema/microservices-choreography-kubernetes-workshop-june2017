{
  "name": "event-bus-publisher",
  "version": "0.0.2",
  "description": "Node.js application that accepts GET requests and published each request as event the event-bus",
  "main": "EventBusPublisher.js",
  "scripts": {
     "start": "node EventBusPublisher.js"
 ,    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "kafka",
    "node",
    "js",
    "produce"
  ],
  "author": "Lucas Jellema",
  "license": "ISC",
  "dependencies": {
    "kafka-node": "^1.3.4",
    "body-parser": "^1.16.1",
    "express": "^4.13.4",
    "request": "^2.81.0"
  }
}
